/**
 * vsup has no typings at all, so we need to declare the module here.
 */
declare module 'vsup' {
  // @TODO Add the actual types for the vsup module.
  export function squareQuantization(): any;
  export function scale(): any;
  
  export const legend: any;
}
